# Frontend (React + TypeScript)

This is a minimal Vite React TypeScript app that will replace the previous static frontend.

To run locally:

```bash
cd frontend
npm install
npm run dev
```

The app connects to the backend Socket.IO endpoint at `/socket.io` and consumes the `/api/summary` endpoint on start.
